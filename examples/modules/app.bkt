module app
import util
import util::double as dbl
import util::square as sq

@test demo() == 42
demo() -> Num "use util::double via alias" {
  dbl(21)
}

@entry
main() -> Num "print util::square(5) via aliases" {
  print(sq(5))
  dbl(3)
}
