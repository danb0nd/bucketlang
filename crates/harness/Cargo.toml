[package]
name = "bucket-harness"
description = "Retrieval, context packing, and the verified edit loop for agentic editing of bucketlang"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "bucket_harness"
path = "src/lib.rs"

[dependencies]
bucketlang = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
